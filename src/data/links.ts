import type { RepoLink } from "@/types/content";

// Empty until the repositories are public. Add entries matching the RepoLink
// type in @/types/content and the Repos tab swaps its empty state for a grid.
export const repoLinks: RepoLink[] = [];
